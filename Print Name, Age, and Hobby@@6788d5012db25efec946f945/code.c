#include <stdio.h>

int main() {
    str name;
    int age;
    str hobby;
    scanf("%s %d %s", &name, &age, &hobby);
    printf("Name: %s\n", name);
    printf("Age: %d\n", age);
    printf("Hobby: %s", hobby);
    return 0;
}